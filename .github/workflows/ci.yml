name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  test-and-gate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .[dev]

      - name: Run unit tests
        run: pytest

      - name: Run fast evaluation gate
        run: python -m app.evals.fast_eval --dataset evals/golden_queries.jsonl --report evals/fast_eval_report.json

      - name: Upload fast evaluation report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: fast-eval-report
          path: evals/fast_eval_report.json
